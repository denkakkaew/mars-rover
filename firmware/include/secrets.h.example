#pragma once

// Copy this file to `secrets.h` and fill in the arena's Wi-Fi credentials.
// `secrets.h` is gitignored — never commit real credentials.

#define WIFI_SSID "your-network"
#define WIFI_PASSWORD "your-password"
